
package mascotas;

public interface CapazDeComer {
    // Todos aquellos que quieran ser capaces de comer deben implementar obligatoriamente el metodo comer()
    // Todos los metodos que aparecen en una interface son publicos, por lo que se puede omitir el modificador de acceso 'public'.
    // Todos los metodos que aparecen en una interface son abstractos, por lo que se puede omitir la palabra 'abstract'.
    void comer();
}
